'use strict';
var assert = require('assert');
var tucha = require('../');

describe('tucha node module', function () {
  it('must have at least one test', function () {
    tucha();
    assert(false, 'done :P');
  });
});
